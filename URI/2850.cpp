#include <bits/stdc++.h>

using namespace std;

int main() {
 char a[9];
 int tam = strlen(a);
 char A[10];

 A = 'esquerda';
 B[1] = 'direita';
 B[2] = 'nenhuma';

  while (scanf("%s", a))
    if(strncmp(a, B[0], tam) == 0)
      printf("ingles");
    else if (strncmp(a, B[1], tam) == 0)
      printf("frances");
    else if (strncmp(a, B[2], tam) == 0)
      printf("portugues");
    else
      printf("caiu");

return  0;
}
